<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="../../helpers/checkuser.js"></script>
<!------ Include the above in your HEAD tag ---------->



<style>

.emp-profile{
    width: 100%;
  padding: 20px 20px;
  margin: 8px 0;
  display: inline-block;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
  margin-bottom: 10%;
}
.profile-img{
    text-align: center;
}
.profile-img img{
    width: 70%;
    height: 100%;
}
.profile-img .file {
    position: relative;
    overflow: hidden;
    margin-top: -20%;
    width: 70%;
    border: none;
    border-radius: 0;
    font-size: 15px;
    background: #212529b8;
}
.profile-img .file input {
    position: absolute;
    opacity: 0;
    right: 0;
    top: 0;
}
.profile-edit-btn{
    border: none;
    border-radius: 1.5rem;
    width: 70%;
    padding: 2%;
    font-weight: 600;
    color: #6c757d;
    cursor: pointer;
}

/* Style the tab */
.tab {
  overflow: hidden;
  border: 1px solid #ccc;
  background-color: #35322d;
  border-radius: 4px;
  color: white;
}

/* Style the buttons inside the tab */
.tab button {
  background-color: inherit;
  float: left;
  border: none;
  outline: none;
  cursor: pointer;
  padding: 14px 16px;
  transition: 0.3s;
  font-size: 17px;
  color: white;
  width: 50%;
}



/* Create an active/current tablink class */
.tab button.active {
  background-color: #ffb03b;
  color: black;
}

/* Style the tab content */
.tabcontent {
  display: none;
  padding: 6px 12px;
  border-top: none;
  width: 80%;
  margin: 0 auto;
  margin-bottom: 10%;
  margin-top: -15%;
}
.rightCont{
  width: 70%;
  float: right;
  margin-right: 3%;
  font-size: 17px;
  margin-bottom: 10%;
}

#displayForm{
  margin-top: 6%;
  padding: 3%;
}

#profileimg{
  min-width: 280px !important;
  min-height: 280px !important;
}

</style>

<div class="FDefaultTab container emp-profile">
<div class="row">
    <div class="col-md-4">
        <div class="profile-img">
          {{#if user.profilepic}}
            <img src="{{user.profilepic}}" id="profileimg" alt=""/>
            
          {{else}}
            <img src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_960_720.png" id="profileimg" alt=""/>
          {{/if}}
            <div class="file btn btn-lg btn-primary">
                Change Photo
                <input type="file" id="userimg" name="userfile" accept="image/png, image/jpeg, image/jpg, image/gif" />
                <input type="hidden" id="profileImg">
            </div>
            <br>
            <span id="errorMsg"></span>
        </div>
    </div>
    <div class="col-md-6">
        <div class="profile-head">
          <h5>
              {{user.fname}} {{user.lname}}
          </h5>
        </div>
        <!-- Tab links -->
        <div class="tab">
          <button class="tablinks active" onclick="openProfile(event, 'About')">About</button>
          {{#ifEquals user.cust_type "customer"}}
          <button class="tablinks" onclick="openProfile(event, 'PD')">Payment Details</button>
          {{/ifEquals}}
        </div>
    </div>
</div>


<div class="tab-content profile-tab" id="myTabContent">
  <div id="About" class="tabcontent">
    <div class="rightCont">
      <div style="float: right;margin-bottom: 2%; display:flex;">
        <a href="/user/editProfile"><button class="FDefaultButtonBlue btn btn-primary ripple-surface" style="background-color: #3b5998;">Update</button></a>
        <form method="POST" action="/user/deleteProfile/{{user.id}}"> 
          <input type="submit" class="FDefaultButtonRed btn btn-secondary" data-mdb-dismiss="modal" value="Delete" style="background-color:#BB4450;">
        </form>
      </div>
      <table width="100%" id="displayInfo">
        <tbody>
          <tr>
            <td align="left">Email</td>
            <td align="right">{{user.email}}</td>
          </tr>
          {{#ifEquals user.cust_type "customer"}}
          <tr>
            <td align="left">Phone No.</td>
            <td align="right">{{user.phone}}</td>
          </tr>
          {{/ifEquals}}
          {{#ifEquals user.cust_type "staff"}}
          <tr>
            <td align="left">Phone No.</td>
            <td align="right"></td>
          </tr>
          <tr>
            <td align="left">Address</td>
            <td align="right">{{restaurant.address}}</td>
          </tr>
          {{/ifEquals}}
          <tr>
            <td align="left">Date Created</td>
            <td align="right">{{user.date}}</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <div id="PD" class="tabcontent">
    <div class="rightCont">
      <div style="float: right;margin-bottom: 2%; display:flex;">
        <a href="/user/editProfile"><button class="btn btn-primary ripple-surface" style="background-color: #3b5998;">Update</button></a>
        <form method="POST" action="/user/deleteProfile/{{user.id}}"> 
          <input type="submit" class="btn btn-secondary" data-mdb-dismiss="modal" value="Delete" style="background-color:#BB4450;">
        </form>
      </div>
      <table width="100%" id="displayInfo">
        <tbody>
          <tr>
            <td align="left">Email</td>
            <td align="right">{{user.email}}</td>
          </tr>
          <tr>
            <td align="left">Phone No.</td>
            <td align="right">{{user.phone}}</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>

<script>
document.getElementsByClassName("tabcontent")[0].style.display = "block";
function openProfile(evt, page) {
  var i, tabcontent, tablinks;
  tabcontent = document.getElementsByClassName("tabcontent");
  for (i = 0; i < tabcontent.length; i++) {
    tabcontent[i].style.display = "none";
  }
  tablinks = document.getElementsByClassName("tablinks");
  for (i = 0; i < tablinks.length; i++) {
    tablinks[i].className = tablinks[i].className.replace(" active", "");
  }
  document.getElementById(page).style.display = "block";
  evt.currentTarget.className += " active";
}


</script>